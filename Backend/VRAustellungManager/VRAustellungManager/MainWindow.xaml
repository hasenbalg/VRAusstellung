<Window x:Class="VRAustellungManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VRAustellungManager"
        mc:Ignorable="d"
        Title="VR Ausstellung Manager" Height="950" Width="525">
    <Window.Resources>
        <DataTemplate x:Key="DataTemplate_Level2">
            <Button Content="{Binding title}" Height="40" Width="50" Margin="4,4,4,4" Click="PieceButton_Click"/>
        </DataTemplate>

        <DataTemplate x:Key="DataTemplate_Level1">
            <ItemsControl ItemsSource="{Binding}" ItemTemplate="{DynamicResource DataTemplate_Level2}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </DataTemplate>

        <Style TargetType="{x:Type Button}">
            <Setter Property="MinWidth" Value="90" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="5" />
        </Style>

       

    </Window.Resources>
    <Grid>
        <StackPanel>
            <StackPanel>
                <TextBox x:Name="ExebName" Text="Titel der Ausstellung"/>
                <TextBlock>
                    Ausstellungsgroesse:
                    <TextBox x:Name="GridWidthTextBox" Text="3"/> × <TextBox Text="3" x:Name="GridHeightTextBox" />
                    <Button Content="OK" x:Name="GridDimensionsChanged_Button" Click="GridDimensionsChanged_Button_Click"/>
                </TextBlock>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image />

                    <Image x:Name="Logo" MaxWidth="100" MaxHeight="100" Grid.Column="0">
                        <!--https://stackoverflow.com/q/25714085-->
                        <Image.Source>
                            <BitmapImage UriSource="Resources/Preview.png" />
                        </Image.Source>
                    </Image>
                    <StackPanel Grid.Column="1">
                <Label Content="Logo der Ausstellung"/>
                        <TextBlock x:Name="ImageFilePath" Text="C://irgendwas"/>
                        <Button Content="Bilddatei auswaehlen" x:Name="ExebLogo_Button" Click="ExebLogo_ButtonClick"/>
                        <Button Content="OK" x:Name="ExebName_Button"/>
                    </StackPanel>
                </Grid>
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.5*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid>
                        <ItemsControl x:Name="PiecesGrid" ItemTemplate="{DynamicResource DataTemplate_Level1}"/>
                    </Grid>


               

                    <Border Grid.Column="1" Background="Aquamarine" BorderBrush="Gainsboro" BorderThickness="1" Margin="5" Padding="5" x:Name="EditPanel">
                        <StackPanel >
                            
                            <TextBox x:Name="IdTextBlock" Visibility="Visible"/>
                            <Label Content="Name"/>
                            <TextBox x:Name="NameTextBox"/>

                            <Label Content="Beschreibung"/>
                            <TextBox x:Name="DescriptionTextBox" TextWrapping="Wrap" AcceptsReturn="True"/>

                            <Label Content="Datei"/>
                            <Button Content="Ausstellungsstueck auswaehlen" x:Name="PieceChoose_Button" Click="PieceChoose_ButtonClick"/>
                            <TextBlock x:Name="PieceFileUrlTextBlock"/>

                            <Image x:Name="PiecePreviewImage" MaxWidth="100" MaxHeight="100">
                                <Image.Source>
                                    <BitmapImage UriSource="Resources/Preview.png" />
                                </Image.Source>
                            </Image>

                            <Button Content="Speichern" x:Name="PieceOKButton" Click="PieceOKButton_Click"/>
                            <Button Content="Loeschen" x:Name="PieceDeleteButton" Click="PieceDeleteButton_Click"/>
                            <Button Content="Abbrechen" x:Name="CancleButton" Click="CancleButton_Click"/>
                        </StackPanel>
                    </Border>
                </Grid>


            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
